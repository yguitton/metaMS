\name{alignmentLC}
\alias{alignmentLC}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
Run XCMS alignment
}
\description{
XCMS alignment by using settings from the XCMSsetting-file
}
\usage{
alignmentLC(xset, XCMSsettings)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{xset}{
%%     ~~Describe \code{xset} here~~
}
  \item{XCMSsettings}{
%%     ~~Describe \code{XCMSsettings} here~~
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
%%  ~Describe the value returned
%%  If it is a LIST, use
%%  \item{comp1 }{Description of 'comp1'}
%%  \item{comp2 }{Description of 'comp2'}
%% ...
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
function (xset, XCMSsettings) 
{
    library(xcms)
    s = XCMSsettings
    xset <- group(xset, minfrac = s$minfrac, minsamp = s$minsamp, 
        bw = s$bw1, mzwid = 0.1)
    xset <- retcor(xset, method = "linear", family = "symmetric", 
        missing = ceiling(0.1 * length(xset@filepaths)), extra = ceiling(0.05 * 
            length(xset@filepaths)))
    xset <- group(xset, minfrac = s$minfrac, minsamp = s$minsamp, 
        bw = s$bw2, mzwid = 0.1)
    if (s$fillPeaks) 
        xset <- fillPeaks(xset)
    return(xset)
  }
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
